class direccion:

    def nombre(self, x=""):
        self.x = x

    def origen(self, x=""):
        self.x = x

    def destino(self, x=""):
        self.x = x

    def sentido(self, x=""):
        self.x = x

    def riesgocalle(self, x=""):
        self.x = x

    def geometria(self, x=""):
        self.x = x
